export const metadata = {
  title: "Freedom Family | Check-In",
  description: "Check-in portal for Freedom Family meetings.",
};

export default function CheckinLayout({ children }) {
  return children;
}
