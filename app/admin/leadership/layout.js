export const metadata = {
  title: "Freedom Family | Leadership Portal",
  description: "Leadership admin portal for Freedom Family meetings.",
};

export default function LeadershipLayout({ children }) {
  return children;
}
